import styled from 'styled-components';

const CompanyName = styled.h4`
  position: sticky;
  top: 0;
  margin: 0;
  padding: 8px 0;
  background: #ffffff;
  font-size: 1.5rem;
  font-weight: 400;
  z-index: 1;
`;

export default CompanyName;
