import styled from 'styled-components';

const Role = styled.h5`
  margin: 32px 0 0;
  padding-bottom: 8px;
  position: sticky;
  top: 2.25rem;
  background: #ffffff;
  font-size: 1.25rem;
  font-weight: 400;
  z-index: 2;
`;

export default Role;
